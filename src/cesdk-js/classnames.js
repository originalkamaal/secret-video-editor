import { classnames } from "@/cesdk-common/classnames";
import { cloneWithPrototypeAndProperties } from "@/cesdk-common/others/createLazyModule";


export var classNames3 = cloneWithPrototypeAndProperties(classnames());
